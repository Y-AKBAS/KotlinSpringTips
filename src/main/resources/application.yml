server:
  port: 8099

spring:
  application:
    name: kotlin-spring
  datasource:
    url: jdbc:postgresql://localhost:5432/kotlin_spring
    driver-class-name: org.postgresql.Driver
    username:
    password:
